import React from 'react';

import { ProductList, Container } from './styles';

const Home = (): JSX.Element => {
  return (
    <Container>
        <ProductList>
     
    </ProductList>
    </Container>
  );
};

export default Home;
